name : buid-deploy 
on : push
jobs: 
    build:
        runs-on: ubuntu-latest
        steps:
            - name: creating file
              run: touch app.txt
    deploy:
        needs: build
        runs-on: ubuntu-latest
        steps:
            - name : checkout uses
              uses: actions/checkout@v6
            - name: reding file
              run: cat app.txt